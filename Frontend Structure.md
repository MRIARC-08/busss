NEXT.JS APP ROUTER FRONTEND
│
├── Component Layers:
│   ├── UI Components    → Buttons, Cards, Badges, Inputs
│   ├── Feature Modules  → RouteSearch, LiveTracker, ReportForm
│   ├── Page Layouts     → Consistent header, nav, footer
│   └── State Management → React useState/useContext + SWR
│
├── Data Flow:
│   User Input → API Call → Response Processing → UI Render
│
└── Styling:
    Tailwind CSS utility classes
    Custom color palette (government blue/orange theme)
    Fully responsive mobile-first design